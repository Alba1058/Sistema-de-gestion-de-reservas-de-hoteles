@model SGHR.Application.DTOs.Reservas.Habitacion.CreateHabitacionDTO

@{
    ViewData["Title"] = "Crear Habitación";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<div class="container-fluid mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-plus-circle"></i> Crear Nueva Habitación
                    </h4>
                </div>
                <div class="card-body">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <strong>Error:</strong> @TempData["Error"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <form asp-action="Create" asp-controller="HabitacionApi" method="post">
                        @Html.AntiForgeryToken()
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label asp-for="Numero" class="form-label fw-bold">Número</label>
                                <input asp-for="Numero" class="form-control" type="number" />
                                <span asp-validation-for="Numero" class="text-danger"></span>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label asp-for="IdCategoria" class="form-label fw-bold">ID Categoría</label>
                                <input asp-for="IdCategoria" class="form-control" type="number" />
                                <span asp-validation-for="IdCategoria" class="text-danger"></span>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label asp-for="IdPiso" class="form-label fw-bold">ID Piso</label>
                                <input asp-for="IdPiso" class="form-control" type="number" />
                                <span asp-validation-for="IdPiso" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="PrecioBase" class="form-label fw-bold">Precio Base</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input asp-for="PrecioBase" class="form-control" type="number" step="0.01" min="0" />
                                </div>
                                <span asp-validation-for="PrecioBase" class="text-danger"></span>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="EstadoHabitacion" class="form-label fw-bold">Estado Habitación</label>
                                <input asp-for="EstadoHabitacion" class="form-control" />
                                <span asp-validation-for="EstadoHabitacion" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="col-md-12 mb-3">
                            <label asp-for="Descripcion" class="form-label fw-bold">Descripción</label>
                            <textarea asp-for="Descripcion" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Descripcion" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-2 mt-4">
                            <button type="submit" class="btn btn-success">
                                <i class="bi bi-check-circle"></i> Guardar
                            </button>
                            <a href="@Url.Action("Index", "HabitacionApi")" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Volver a la Lista
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}